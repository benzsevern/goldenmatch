FROM python:3.12-slim
# cache-bust: v1.4.4 (empty resources/prompts handlers)
WORKDIR /app

COPY . .
RUN pip install --no-cache-dir ".[mcp]"

EXPOSE 8200

CMD ["goldenmatch", "mcp-serve", "--transport", "http", "--port", "8200"]
